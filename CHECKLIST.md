# QuickPick（极选）用户选片 MVP Checklist

> 最后更新时间：2026-09-09
> 当前定位：面向已经从摄影师处取得本地 RAW/JPEG 原片的普通用户，帮助其从上千张照片中快速完成选择、复核和安全导出。
> 最高安全原则：源照片和源目录在扫描、浏览、分析、选择过程中必须保持只读；应用不得修改、移动、覆盖或删除源照片，也不得默认在源目录写入 XMP、缓存、锁或项目文件。

## 勾选规则

- `[x]`：功能已经接入当前主流程，并有自动化测试或明确的可重复验收结果。
- `[ ]`：尚未实现、只有代码骨架、仍依赖旧兼容层，或缺少关键验收。
- 单个底层函数存在，不代表完整用户功能完成。
- 本清单以发布安全和端到端用户闭环为准，不再以摄影师、NAS、XMP 工作流的旧完成度计算。

## 当前结论

| 阶段 | 目标 | 严格状态 | 发布判断 |
| :--- | :--- | :---: | :--- |
| P0 | 源照片只读与可靠项目恢复 | 部分完成 | 仍需只读介质验收 |
| P1 | 千张照片快速打开与稳定预览 | 部分完成 | 阻断公测 |
| P2 | 用户选片与复核闭环 | 部分完成 | 不构成完整 MVP |
| P3 | 安全复制与清单导出 | 已完成自动化实现 | 待真实介质验收 |
| P4 | 测试、性能与跨平台发布 | 未完成 | 阻断发布 |

当前进展：扫描路径 ID 与用户记录使用的永久 `photo_key` 已分离，SQLite 可通过路径、大小、mtime 和轻量内容指纹处理重命名、替换、新增与缺失照片；项目浏览状态、周期备份、异常退出检测、损坏恢复、最近项目入口和源文件夹重新定位已接入。桌面缓存、项目、索引和分析状态均位于应用数据目录，旧 Rating/Color Label/Pick/Reject/XMP 写入领域与 IPC 已移除；现存 XMP 能力仅为导出时只读复制已有伴侣文件。P0 仍需操作系统只读目录/介质的真实验收。

---

## P0：发布安全红线

### P0.1 源照片和源目录严格只读

- [x] 删除桌面端 `update_triage` Tauri command
- [x] 删除桌面端 `save_triage_conflict_copy` Tauri command
- [x] 删除前端 `updatePhotoTriage` 和 `savePhotoTriageConflictCopy` 桥接接口
- [x] 主流程不读取 XMP 中的 QuickPick 选择状态作为用户决定
- [x] 扫描、预览、分析和选择过程中不创建或修改 XMP
- [x] 缓存 API 强制使用独立缓存根目录，不再把 `.quickpick_cache` 写入源目录
- [x] Docker/NAS 照片卷改为只读挂载，代理缓存使用独立 `/cache` 卷
- [x] 桌面缓存、索引、洞察和项目文件全部迁移至 Tauri `app_data_dir`
- [x] 桌面主流程不在源目录创建锁、临时文件、备份或导出子目录
- [x] Tauri 文件系统 capability 仅声明读取目录和读取文件权限
- [x] 建立源目录只读集成测试：操作前后文件列表、内容 Hash、大小、mtime 和权限完全一致
- [x] 在 Unix 权限只读目录中自动化验证扫描、预览缓存、分析、选择和重启恢复
- [ ] 在真实只读介质及 Windows 只读目录上完成同一流程验收

验收标准：除用户明确选择的导出目标目录外，应用对源目录不产生任何写操作。

### P0.2 稳定照片身份

- [x] 移除扫描阶段 `Uuid::new_v4()` 随机照片 ID
- [x] 在项目数据库中为首次发现的照片分配稳定 `photo_key`
- [x] 使用相对路径、大小、mtime 等信息重新关联已有照片
- [x] 同名或关联歧义时使用内容指纹确认，不在首次打开时全量计算大文件 Hash
- [x] 支持源文件夹重命名或移动后的“重新定位”
- [x] 明确处理新增照片、删除照片、重命名照片和同名替换
- [x] 自动化测试：连续扫描同一目录得到完全一致的照片身份
- [x] 自动化测试：重启应用后选择记录仍准确对应原照片

验收标准：关闭并重新打开应用不会丢失或错配任何选择、备注和查看进度。

### P0.3 项目与选择记录可靠持久化

- [x] 引入 SQLite 项目数据库，存放于应用数据目录
- [x] 建立 `projects` 表：源目录、相册名称、当前位置、目标数量、创建和更新时间
- [x] 建立 `photos` 表：稳定 ID、相对路径、大小、mtime、格式和拍摄时间
- [x] 建立 `selections` 表：当前使用 `unreviewed/selected/maybe/skipped`、备注和更新时间
- [x] 建立 `review_progress` 表：独立保存是否查看和查看时间
- [x] 每次选择变更使用数据库事务落盘
- [x] 数据库写入失败必须在 UI 明确提示，并禁止伪装成“已保存”
- [x] 增加 schema version 和数据库迁移机制
- [x] 增加周期性恢复快照或备份
- [x] 启动时检测未正常关闭和损坏数据库
- [x] 支持继续最近项目
- [x] 恢复上次浏览位置、筛选状态、目标数量和场景划分
- [x] 自动化测试：关闭数据库连接并重新打开后，选择、备注和查看进度完整恢复
- [x] 自动化测试：数据库损坏时可以从备份恢复或安全导出已有决定

验收标准：选择结果不依赖 WebView `localStorage`；任何持久化失败都不会静默发生。

### P0.4 新旧架构收口

- [x] 已建立 `albumStore.ts`
- [x] 已建立 `selectionStore.ts`
- [x] 已建立 `previewStore.ts`
- [x] 已建立 `compareStore.ts`
- [x] 已建立 `insightStore.ts`
- [x] 已建立 `exportStore.ts`
- [x] 所有界面组件直接使用对应领域 store
- [x] `Filmstrip` 移除对旧 `usePhotoStore` facade 的依赖
- [x] `FaceLoupe` 移除对旧 `usePhotoStore` facade 的依赖
- [x] 删除 `photoStore.ts` 中的旧接口、空实现和兼容字段
- [x] 删除 Rating、Color Label、Pick/Reject、XMP conflict 等旧领域概念
- [x] 扫描元数据、分析提示和用户决定在 Rust/TypeScript 边界保持分离且字段一致

验收标准：应用中只有一套照片、选择、预览、比较和导出状态源。

---

## P1：快速打开与稳定预览

### P1.1 分层扫描

- [x] 支持 ARW、CR3、CR2、NEF、DNG、RAF、ORF、RW2、PEF、SRW
- [x] 支持 JPEG、PNG、WebP
- [x] 支持按文件名排序
- [x] 第一阶段只枚举路径、文件名、大小和格式，不进行全量分析
- [x] 文件列表就绪后立即进入工作区
- [x] EXIF 提取改为后台增量任务
- [x] 相似组计算改为后台增量任务
- [x] 闭眼、模糊等辅助检查改为后台增量任务
- [x] 后台任务支持取消、去重和有界并发
- [x] 切换相册时取消上一相册所有任务
- [x] 单张损坏或不支持的文件不得阻塞整个相册
- [x] 扫描错误必须显示用户可理解的提示和重试入口

验收标准：3000 张照片目录在 2 秒内展示可操作文件列表；分析不阻塞首张预览。

### P1.2 RAW/JPEG 预览

- [x] 已接入 LibRaw 内嵌预览提取
- [x] JPEG/PNG/WebP 可直接读取预览
- [x] 当前照片预览具备加载、成功、失败和重试状态
- [x] 前端具备相邻照片预加载骨架
- [x] 缓存迁移到应用数据目录
- [x] 缓存 key 包含稳定照片身份和源文件版本信息
- [x] 相同预览请求并发去重
- [x] 相册切换后过期 Promise 不得污染新相册
- [x] 缓存淘汰同时释放 Pixi/WebGL texture
- [x] 预览缓存具备容量和磁盘空间上限
- [x] RAW 视口明确显示内嵌预览尺寸和“非完整 RAW 像素”提示
- [ ] 真实 RAW 机型矩阵验收

验收标准：本地 SSD 首张可用预览目标小于 1 秒，连续翻页 P95 小于 100ms，长时间浏览内存和显存不持续增长。

### P1.3 性能与包体

- [x] 当前 TypeScript 和 Vite 生产构建通过
- [x] 胶片条已具备虚拟化渲染骨架
- [x] 入口 bundle 降至约 226 KB，所有 JavaScript chunk 均低于 500 KB
- [x] Pixi 视口、双图比较、导出弹窗、人脸窗格按需加载
- [ ] 建立 1000/3000/5000 张照片基准数据集
- [ ] 记录首列表、首预览、连续翻页、峰值内存和峰值显存
- [ ] 性能基准使用真实 RAW/JPEG，不使用小型合成 JPEG 冒充真实客片

---

## P2：用户选片与复核闭环

### P2.1 首页与项目入口

- [x] 空状态提供“选择照片文件夹开始选片”入口
- [x] 首页文案已基本转向普通用户本地选片
- [x] 展示最近项目
- [x] 展示项目照片总数、已查看数、已选数和最后使用时间
- [x] 支持继续最近项目
- [x] 源目录失效时提供重新定位，不自动创建空项目
- [x] 首次打开明确说明源照片只读和数据保存位置

### P2.2 第一轮快速选择

- [x] `Space` 选择/取消选择
- [x] `M` 标记待考虑
- [x] 左右方向键翻页
- [x] `Cmd/Ctrl+Z` 撤销
- [x] 主界面展示已查看、已选和待考虑数量
- [x] 已选择和待考虑状态在胶片条有视觉标识
- [x] 取消选择恢复为未决定状态，不再隐式变成 `skipped`
- [x] “查看过但未决定”与“从未查看”分开记录
- [x] 提供明确的“跳过/不选”动作
- [x] 选片状态变更后展示保存中或保存失败状态
- [x] 撤销操作通过数据库事务落盘，失败时恢复撤销前状态
- [x] 快捷键帮助可在应用内查看
- [x] 非技术用户可完全通过鼠标完成同一流程

### P2.3 筛选和遗漏检查

- [x] 已有全部、未查看、已选择、待考虑、建议检查、相似连拍入口
- [x] “未查看”严格使用 `review_progress`，不再使用选择状态代替
- [x] 筛选后的前后翻页只在筛选结果内移动
- [x] 当前照片不匹配新筛选时自动跳到首个匹配项
- [x] 显示筛选结果中的当前位置和总数
- [x] 复核中心集中展示待考虑、未查看、相似组和辅助检查
- [x] 导出前提示未查看数量和待考虑数量
- [x] 允许用户确认仍有未查看照片并继续导出，但不得静默忽略

### P2.4 相似连拍与双图比较

- [x] 已有双图分屏比较视图
- [x] 优先选择同一连拍组候选
- [x] 已有选左、选右、都选、都待考虑的 store 操作
- [x] 支持在同组候选间导航
- [x] 比较操作全部接入新的持久化事务
- [x] 相似组 ID 使用稳定照片身份，重启后保持一致
- [x] 基于 64-bit DCT pHash 感知哈希升级为“时间戳 + 文件名 + 视觉相似度”混合聚类
- [x] 连拍组内基于锐度与人脸睁眼度评定并标注“组内推荐最佳”
- [x] 双图比较实时显示两图视觉相似度百分比与推荐最佳标记
- [x] 不因选择一张而自动排除组内其他照片
- [x] 提供“下一相似组”完整 UI
- [x] 对比预览加载失败不影响已有选择
- [ ] 双图缩放、平移同步经过真实高分辨率图片测试

### P2.5 辅助提示边界

- [x] `PhotoInsight` 与 `UserSelection` 已在前端类型上分离
- [x] 当前 UI 将闭眼、模糊和相似结果表达为辅助检查
- [x] 后端模型不再输出 `clean/fixable/fatal` 作为用户决定
- [x] 删除 AI 批量采纳和批量排除能力
- [x] 辅助提示永远不能自动改变选择状态
- [x] 分析失败显示“无法分析”，不得显示“未见问题”
- [ ] 使用真实照片验证闭眼、模糊提示的误报和漏报
- [x] 未完成真实验证前，不在产品文案中宣称可靠 AI 判片

---

## P3：安全导出

### P3.1 导出领域模型

- [x] 已有复制所选原片入口
- [x] 已有 TXT、CSV、JSON 清单内容生成骨架
- [x] 从前端 `ExportOptions` 删除 `is_move`
- [x] 从前端 `ExportOptions` 删除 `overwrite`
- [x] 从 Rust `ExportOptions` 删除 `is_move`
- [x] 从 Rust `ExportOptions` 删除 `overwrite`
- [x] 导出目录不再默认放在源照片目录内部，必须由用户明确选择
- [x] 后端在任何目标写入前禁止源目录或其任意子目录作为导出目标
- [x] 清单使用原生保存对话框和 Rust 文件写入，不依赖浏览器 download
- [x] MVP 移除 `preview_jpg` 社交媒体预览导出

### P3.2 导出前检查

- [x] 显示待复制文件数量和总大小
- [x] 检查目标可用空间
- [x] 检查源路径是否仍存在且为普通文件
- [x] 已有规范路径和部分符号链接检查
- [x] Unix 下已有同 inode 硬链接检查
- [x] 检查选择列表内重复源文件
- [x] 列出所有目标同名冲突
- [x] 冲突策略仅允许安全跳过或生成唯一名称，不允许覆盖
- [x] 用户确认后才创建导出任务

### P3.3 事务化复制

- [x] 当前后端拒绝执行移动源文件
- [x] 当前后端拒绝覆盖现有目标文件
- [x] 使用目标侧临时文件
- [x] 已实现源文件和临时目标文件 SHA-256 校验
- [x] 校验成功后使用目标侧 rename 提交
- [x] 已有导出 manifest 骨架
- [x] 导出任务状态保存到应用数据库
- [x] 每个文件记录 pending/copying/verified/committed/skipped/failed
- [x] 复制前后检查源文件大小和 mtime，发现外部变化则终止该文件
- [x] 取消任务只清理本任务创建的目标临时文件
- [x] 崩溃重启后可识别、恢复或安全清理未完成任务
- [x] 导出结果明确区分成功、跳过、失败和未处理
- [x] 导出结束后允许在系统文件管理器打开目标目录

验收标准：任何失败、中断、掉线或磁盘空间不足都不得修改源文件，也不得覆盖目标目录已有文件。

### P3.4 清单导出

- [x] 支持 TXT 文件名列表内容
- [x] 支持 CSV 内容
- [x] 支持 JSON 内容
- [x] CSV 使用可靠转义并包含 UTF-8 BOM 或明确编码策略
- [x] 清单包含项目名称、生成时间、选中数量和相对路径
- [x] 同名文件来自不同子目录时能够区分
- [x] 清单写入采用临时文件、fsync 和原子提交
- [x] 清单保存失败有明确提示和重试入口

---

## P4：测试与发布门禁

### P4.1 自动化测试

- [x] Rust 单元测试框架已接入
- [x] 当前前端生产构建通过
- [x] 引入 Vitest
- [x] 引入 React Testing Library
- [x] 为六个领域 store 添加单元测试
- [x] 测试筛选导航和未查看统计
- [x] 测试快捷键不会在输入框中误触发
- [x] 测试相册切换期间的异步预览竞态
- [x] 测试项目恢复和稳定照片身份
- [x] 测试所有选择状态和撤销事务
- [x] 测试导出 preflight 和冲突策略
- [x] 测试源目录零变化安全不变量

### P4.2 故障注入

- [x] 扫描过程中源磁盘断开
- [x] 预览读取过程中照片被外部移动
- [x] 数据库写入失败
- [x] 应用在选择提交过程中崩溃
- [x] 导出目标空间不足
- [x] 导出目标掉线或变为只读
- [x] 导出过程中应用被强制终止
- [x] 源文件在复制过程中被外部修改
- [x] 目标出现同名文件、符号链接和硬链接别名
- [x] 所有故障场景验证源照片内容不变

### P4.3 真实素材验收

- [ ] Sony ARW 真实样本
- [ ] Canon CR3/CR2 真实样本
- [ ] Nikon NEF 真实样本
- [ ] Fujifilm RAF 真实样本
- [ ] DNG 真实样本
- [ ] 普通 JPEG/PNG/WebP 样本
- [ ] 损坏和截断文件样本
- [ ] 1000、3000、5000 张混合素材目录
- [ ] 至少一次完整普通用户可用性测试：打开、选择、复核、关闭恢复、导出

### P4.4 跨平台和分发

- [x] macOS Apple Silicon 构建和安装验证
- [ ] macOS Intel 构建和安装验证
- [ ] Windows x64 构建和安装验证
- [ ] macOS 公证
- [ ] Windows 代码签名
- [x] 安装包携带 LibRaw/LGPL 许可和替换说明
- [ ] 安装、升级和卸载不得删除项目数据库，除非用户明确选择清除数据
- [x] 升级前自动备份项目数据库

---

## MVP 完成定义

以下条件必须全部满足，才可以将 MVP 标记为完成：

- [ ] 用户可以打开摄影师交付的上千张本地 RAW/JPEG 原片
- [ ] 文件列表和首张预览在目标性能内可用
- [ ] 用户可以选择、待考虑、跳过、撤销和添加备注
- [ ] 用户可以复核真正未查看的照片和相似连拍
- [ ] 关闭或崩溃后重新打开，所有选择和进度准确恢复
- [ ] 应用从不修改、移动、覆盖或删除源照片
- [ ] 应用不在源目录写 XMP、缓存、锁或项目文件
- [ ] 用户可以安全复制所选原片，复制结果经过 SHA-256 校验
- [ ] 用户可以原生保存 TXT/CSV/JSON 选片清单
- [ ] 所有 P0 安全测试、故障注入和真实素材验收通过
- [ ] macOS 和 Windows 至少各有一个受支持安装包完成验证

## MVP 后再评估

- [ ] 社交媒体 JPEG 导出：sRGB、方向、尺寸、质量、GPS 清理和水印
- [ ] 完整 RAW 显影和色彩管理
- [ ] 更可靠的人脸、闭眼、锐度和相似度模型
- [ ] 多项目比较和选择方案版本
- [ ] 摄影师 XMP/Lightroom 工作流扩展
- [ ] NAS、Docker 和多人协作扩展
- [ ] 客户移动端协同选片小程序与商业化云扩展（详见独立方案设计：[TODO.md](TODO.md)）
