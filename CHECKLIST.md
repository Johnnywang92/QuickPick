# QuickPick（极选）研发任务全景 Checklist

> 最后更新时间: 2026-09-08  
> 对照基准文档: [DEVELOPMENT_PLAN.md](./DEVELOPMENT_PLAN.md) (V3.1 锁定版)

---

## 📊 阶段进度概览

| 研发阶段 | 核心目标 | 完成度 | 状态 |
| :--- | :--- | :---: | :---: |
| **阶段 0：前置技术验真** | LibRaw 动态链接、环境配置、真实底片矩阵 | **85%** | 🟢 基础闭环 |
| **阶段 1：MVP 极速选片引擎** | 60fps 渲染画布、XMP 哨兵锁、盲打状态机、选片导出器、双图分屏比对、工程门禁与分发 | **100%** | 🟢 完备闭环 |
| **阶段 2：AI 规则与智能打标签** | “可修/不可修”分类、连拍选优、多脸特写窗格 (Face Loupe)、场景预设 | **95%** | 🟢 高度完备 |
| **阶段 3：NAS 协同与工作室生产力** | Docker 后台服务、2K 代理缓存规范、零 RAW 传输加速 | **90%** | 🟢 架构闭环 |

---

## 发布阻断整改清单（按优先级）

> 本节记录“真实客片可安全使用”的验收门槛。下方阶段清单中的 `[x]` 仅表示功能骨架存在；若与本节冲突，以本节验收结果为准。

### P0：数据安全与可信判定（发布前必须完成）

- [x] **P0.1 XMP 保真更新**：只合并 QuickPick 负责的评星、色标与自定义字段，不得删除 Lightroom / Capture One 已有的调色、关键词、版权及相机元数据；首次修改保留可恢复备份
- [x] **P0.2 XMP 冲突检测闭环**：扫描时记录完整文件 Hash / Rev，持锁后重新读取并比较；冲突时禁止静默覆盖，并向用户展示“重新载入 / 保留本地 / 另存副本”
  - [x] 完整文档 Hash 基线、持锁复核和禁止静默覆盖
  - [x] 冲突后的重新载入、保留本地和另存副本交互
- [x] **P0.3 锁所有权安全**：锁守卫仅能删除自身 Token 的锁；覆盖超时锁、异常锁、休眠恢复和双客户端并发均有自动化测试
  - [x] 锁 Token 所有权校验及写入临时文件隔离
  - [x] OS 协调锁保护的超时锁接管、稳定客户端 UUID 和双客户端并发测试
- [x] **P0.4 未分析状态可信化**：新照片保持 `pending/待分析`；解码或分析失败必须标为 `unknown/failed`，禁止回退成“完美原片”
  - [x] 新照片与失败分析不再伪装成“完美原片”，筛选与状态提示支持待分析
  - [x] 为解码/分析失败增加独立的 `failed` 状态及重试入口
- [ ] **P0.5 高风险批量操作保护**：AI 批量采纳/排除只作用于已完成分析且达到置信度阈值的照片，执行前展示范围预览，失败可重试/回滚
  - [x] 排除待分析照片、执行前确认、逐张落盘及部分失败汇总
  - [ ] 置信度阈值配置和失败重试
  - [x] 单次批量操作记录成功落盘范围，并支持整批撤销；部分失败只撤销实际成功项
  - [x] 移除前端原生 `window.confirm` / `window.alert` 线程阻塞调用，接入原生对话框组件 (`tauriBridge.ts: confirmAction/showAlert`)
- [x] **P0.6 导出事务化**：照片与 XMP 成对暂存、校验后提交；移动采用“复制 + 校验 + 删除源文件”，支持跨卷，任何一步失败不得留下半套结果
- [x] **P0.7 写入状态与撤销**：界面明确显示保存中、已保存、保存失败；失败回滚乐观状态，并支持 `Cmd/Ctrl+Z`
  - [x] 保存状态、错误提示和单张失败回滚
  - [x] 写入异常/XMP 冲突时阻断 AutoAdvance 自动跳张（已修复错误时仍强行翻页缺陷）
  - [x] 100 步操作历史栈、工具栏入口及 `Cmd/Ctrl+Z` 撤销；撤销本身通过 XMP Hash 冲突检测安全落盘

### P1：性能、工作流与跨平台（公开测试前完成）

- [x] **P1.1 真实连拍聚类**：引入 EXIF 拍摄时间戳 (`date_time_original`) 严格判定相邻照片间隔（≤ 2秒），消除了快门文件名回卷与跨日跨时段的错误连拍聚类；缺失时间戳时平滑降级至序号差值 (`rules/mod.rs`)
- [x] **P1.2 显存防爆与缩略图虚拟化**：
  - [x] **PixiJS 显存泄漏治理**：`photoStore` 驱逐缓存时联动调用 `Assets.unload(url)`，重绘前销毁旧 Sprite，避免翻页显存持续累积
  - [x] **胶片栏虚拟滚动**：基于屏幕视口动态计算可视区间，DOM 节点恒定保留 15~20 个，支持 10 万级超大相册零延迟秒开与平滑自动居中
- [ ] **P1.3 预览管线验收**：
  - [x] 修复 Pixi 异步初始化/首图加载竞态，预览提取与纹理加载失败均有占位和重试
  - [x] “1:1”明确标注为当前内嵌/代理预览像素，不冒充完整 RAW 像素
  - [ ] 使用真实 RAW 与快速连续翻页场景完成回归和性能验收
- [x] **P1.4 跨平台 LibRaw 分发**：移除固定 Apple Silicon target 和 Homebrew 绝对依赖，安装包携带动态库、LGPL 文本及可替换说明
  - [x] 移除 `src-tauri/.cargo/config.toml` 中强行锁死的 `aarch64-apple-darwin`，自适应宿主机架构与交叉编译
  - [x] `src-tauri/build.rs` 引入 `@executable_path/../Frameworks` 优先 RPATH 与 `LIBRAW_DIR` 环境变量支持，杜绝普通用户无 Homebrew 运行崩溃
  - [x] 补充 `LICENSES/LGPL-2.1.txt`、`LICENSES/LIBRAW_LICENSE.txt` 以及完整替换指南 `docs/LIBRAW_REPLACEMENT.md`
  - [x] 前端集成“关于 QuickPick 与开源合规 (`AboutModal`)" 交互弹窗，支持协议与替换指南内嵌查阅
- [x] **P1.5 导出/双图比对体验**：
  - [x] **双图对比候选导航跳过主选片**：`nextCompareCandidate`/`prevCompareCandidate` 遇到当前主选片 `currentIndex` 时自动越过，彻底消除死锁卡顿
  - [x] **连拍组内限定比对与筛选相对序号**：支持 `compareScope: 'burst'` 组内环形比对与候选 `[X/N]` 显示；顶部与 HUD 清晰呈现 `筛选: 3/45 (全局 #48/1200)`
- [x] **P1.6 工程门禁**：前端 build、Rust test、Clippy、XMP 兼容样本和导出故障注入进入 CI；建立首屏、翻页、峰值内存基准
  - [x] 修复全量 Clippy 警告与指针解引用错误，实现 `cargo clippy --all-targets -- -D warnings` 零警告零容忍通过
  - [x] 建立 GitHub Actions CI 流水线 (`.github/workflows/ci.yml`)，覆盖 TypeScript、生产构建、Clippy、27 项测试与体积/基准
  - [x] 制定性能红线与质量基准规范 (`docs/PERFORMANCE_BASELINES.md`) 与自动化基准核验脚本 (`scripts/check_baselines.js`)
  - [x] `package.json` 整合 `npm run gate` 一键全量工程门禁校验（集成前端构建、Clippy、Rust 测试与 Baseline）

### P2：差异化能力（P0/P1 稳定后推进）

- [x] **P2.0 场景预设模式引擎 (Scene Workflow Engine)**：
  - [x] 5 大摄影专业场景引擎：`general` (通用人像), `concert` (演唱会/舞台演出), `cosplay` (二次元/Cosplay/漫展), `conference` (商业活动/会议公关/图片直播), `wedding` (婚礼纪实)
  - [x] 场景专属差异化 AI 规则库与动态阈值：
    - 演唱会舞台模式 (`concert`)：歌手单人沉浸表演闭眼识别并豁免为非缺陷；舞台爆闪高光严重死白严格预警 (`highlight > 0.14`)；放宽舞台烟雾/追光临界合焦
    - 二次元/Cosplay 模式 (`cosplay`)：最严苛美瞳、假毛与眼妆合焦锐度质检 (`sharpness < 35` 致命, `< 55` 临界复核)；神态与张力优先
    - 商务会议模式 (`conference`)：大合影全员睁眼严格判定 (`confidence: 0.98`)；领导致辞嘴型崩坏防雷
    - 婚礼纪实模式 (`wedding`)：新人感动流泪/大笑情绪抓拍高容忍度；连拍换脸/换眼跨帧拯救深度提示
  - [x] 前端响应式体验：`FilterToolbar` 场景切换下拉框与 `localStorage` 持久化，视口 HUD 与诊断卡片实时展示场景模式徽标与豁免标签，免重新解码 RAW 即时响应 (<1ms)
- [x] **P2.1 不确定性优先队列（争议片集中裁决）**：
  - [x] 后端规则冲突与临界判定：合影闭眼分歧 (`review_group_blink_conflict`)、合焦边缘临界度 (`review_borderline_sharpness`) 与连拍换脸候选检测
  - [x] 前端集中裁决队列：`FilterCategory` 增加 `'review'`，顶部工具栏设独立 `待定复核` Tab 与 `[仅看未裁决]` 快捷过滤
  - [x] 多级视觉感知：胶片条、HUD 与诊断卡片同步呈现 `⚖️ 待定复核` 徽标并提示具体争议原因（如合影闭眼分歧、主角闭眼、临界合焦等）
  - [x] 智能批量操作安全豁免：`batchPickClean`、`batchRejectFatal`、`applyAiSuggestions` 自动排除争议待复核照片，防止误采纳/误剔除并弹窗明确提示豁免张数
- [x] **P2.2 活动/演出流程时间轴分章与交付配额看板**：
  - [x] 自动拍摄时间密度聚类：Rust 后端与前端双引擎根据 EXIF 时间戳差值（默认 10 分钟，支持 5~60 分钟可调）自动划分子章节，无时间戳平滑降级
  - [x] 场景自适应流程模板库：针对婚礼（早妆/迎亲/敬茶/外景/主仪式/晚宴）、演唱会（开场/主打/慢歌/MC/高潮/安可）、商业会议（签到/主旨/论坛/茶歇/合影/成果）及二次元Cosplay等自动匹配环节名称与建议交付配额
  - [x] 交付配额大盘与空缺警报：实时汇总总入选/目标配额比，针对入选数为 0 的空白环节亮起鲜红“空缺预警”，防止活动关键仪式/环节漏交付
  - [x] 全链路视觉与交互感知：
    - 顶栏流程配额概览胶囊与空缺预警脉冲红点
    - 视口参数 HUD 显示所属章节与交付达成进度（`📖 02. 主旨演讲 [8/10]`）
    - 胶片条虚拟轨道渲染彩色章节起始竖条与标题微标
    - 筛选工具栏支持按章节沉浸式隔离选片，支持单环节拆分、合并与就地重命名
    - 章节与配额数据依据相册目录自动持久化至 `localStorage`
- [x] **P2.3 连拍冠军淘汰赛**：双图组内限定候选、长按 `[B]` 键瞬时闪烁比对、一键定优（`W` 设为 5星 Pick 并自动将同组其余设为 Reject）、支持单次 `Cmd+Z` 完整撤销与 XMP 事务安全落盘
- [ ] **P2.4 双机位时间同步**：估算相机时钟偏差，把同一瞬间的多机位照片归组
- [ ] **P2.5 可解释与本地校准**：
  - [x] 归一化主角钉选加权与连拍正向继承
  - [x] 连拍取消钉选双向同步（已修复取消钉选时向同连拍组广播同步）
  - [ ] 展示人脸裁剪、锐度/曝光依据和置信度，利用误报反馈形成本地相机/镜头配置
- [ ] **P2.6 项目会话与安全快照**：SQLite 保存索引、代理、操作日志和 XMP 基线，支持崩溃恢复与筛选方案版本

---

## 阶段 0：前置技术验真与基线工程 (Phase 0)

- [x] **0.1 LibRaw LGPL 动态链接**：搭建 Rust FFI 基础脚手架，支持 `pkg-config` 动态探测与 macOS Homebrew rpath 注入 (`build.rs`)
- [x] **0.1 动态库探活测试**：完成 `test_libraw_version` 单元测试，成功动态链接 LibRaw 0.22.2 (`lib.rs`)
- [x] **0.1 工具链架构适配**：移除固定 Apple Silicon 限制，支持宿主机架构自适应与跨平台构建 (`src-tauri/.cargo/config.toml`)
- [x] **0.1 应用图标资源生成**：生成完整标准 Tauri 图标集 (`src-tauri/icons/`)，解决 `tauri::generate_context!()` 构建报错
- [x] **0.2 真实底片端到端测试**：集成测试真实样例照片 (`test_fixtures/`) 扫描、指标计算与打标 (`test_scan_directory_and_rules`)
- [ ] **0.2 多机型预览矩阵报告**：获取 Sony A7M4/A7R5、Canon R5/R6、Nikon Z8/Z9 真实 RAW 样本进行内嵌预览提取延迟与分辨率统计
- [ ] **0.3 降级去马赛克耗时实测**：实现并实测老旧机型降级分支 `user_qual=0 + half_size=1` 在本地 SSD 与 NAS SMB 下的耗时
- [ ] **0.4 摄影师工作流调研**：访谈 6~8 位一线商业摄影师/修图师，验证 NAS 协同真实痛点与使用频次

---

## 阶段 1：MVP 核心极速选片引擎 (Phase 1)

### 1.1 底层引擎与 IO 管线 (Rust)
- [x] **目录扫描器**：支持常见无反 RAW (`.arw`, `.cr3`, `.nef`, `.dng` 等) 与普通图片格式扫描与自然排序 (`engine/mod.rs`)
- [x] **RAW 内嵌图极速提取**：调用 `libraw_dcraw_make_mem_thumb` 实现 5~15ms 极速提取内嵌预览 (`libraw_ffi/mod.rs`)
- [x] **XMP 伴侣读写**：标准 Adobe 评级/色标/标记 XML 解析与原子写入 (`xmp/mod.rs`)
- [x] **内容哈希与防冲突**：引入 SHA-256 内容指纹与 `quickpick:rev` 递增版本号，防御 SMB 时钟漂移与假死
- [x] **原子哨兵文件锁**：`.photo.xmp.lock` 租约锁机制，支持自适应超时与防僵尸覆盖 (`write_xmp_atomic_full`)
- [x] **照片拍摄参数 (EXIF) 提取引擎**：双引擎支持 RAW (LibRaw C 访问器) 与 JPG/TIFF (纯 Rust APP1 IFD) 极速提取机身型号、镜头型号、光圈、快门、ISO、焦段与拍摄时间戳，并持久化到 catalog 缓存 (`engine/exif.rs`, `libraw_ffi/mod.rs`)
- [x] **XMP 自定义元数据扩展**：持久化 `<quickpick:retouchStatus>`、`<quickpick:defectTags>`、`<quickpick:burstGroupId>`
- [x] **选片结果批量导出引擎**：支持大文件高效流式拷贝/移动、XMP 伴侣成对同步、防覆盖策略 (`engine/export.rs`) *(本次完成)*
- [x] **系统文件管理器联动**：支持一键在 macOS Finder / Windows 资源管理器中打开目标文件夹 (`reveal_directory`) *(本次完成)*
- [ ] **锁心跳续租线程**：持锁写入临界区内后台 1.5s 周期心跳续约
- [ ] **灾后自愈机制**：接管超时锁后自动清理残留 `.tmp` 临时文件与 XML 语法完整性校验

### 1.2 前端界面与渲染层 (React + Pixi.js)
- [x] **WebGL 加速视口**：基于 Pixi.js v8 的硬件加速视口，实现照片毫秒级平滑渲染 (`PixiCanvas.tsx`)
- [x] **视口拍摄参数 HUD**：主视口左上角悬浮半透明胶囊，优雅展示相机型号、商业镜头型号、物理/等效焦段、光圈、快门速度、ISO及拍摄时间，支持一键折叠/展开 (`PhotoInfoHud.tsx`)
- [x] **双图分屏曝光参数比对**：在双图比对视口标题浮标中直接显示两张候选底片各自的焦段与曝光三要素 (`SplitCompareView.tsx`)
- [x] **平滑交互操作**：滚轮缩放、鼠标抓手拖拽平移、适应窗口、1:1 像素无损点对点查看
- [x] **胶片缩略图虚拟化轮播栏**：基于可视窗口动态虚拟滚动，DOM 节点恒定 ~20 个，选中项数学计算平滑居中 (`Filmstrip.tsx`)
- [x] **胶片状态微标**：缩略图叠加智能红黄绿诊断圆点及连拍“连拍”标签，配合筛选视图透明度高亮
- [x] **内存环形预取缓冲**：当前张立即呈现 + 异步预取 Next 1/2、Prev 1，实现翻页无感知延时 (`photoStore.ts`)
- [x] **内存防爆回收策略**：滑动窗口保留最新 35 张纹理缓存，超额联动调用 PixiJS `Assets.unload(url)` 彻底释放 WebGL 显存，重绘前显式销毁旧 Sprite
- [x] **盲打快捷键状态机**：`0-5` 评星、`P/X/U` 状态、`6-9` 色标、`J/K/左右箭头` 翻页 (`useKeyboardShortcuts.ts`)
- [x] **自动跳张选片模式**：打标后自动翻至下一张（Caps Lock 极速初选模式）
- [x] **顶部分类筛选栏**：全部、🟢完美原片、🟡可修解决、🔴不可修硬伤、⭐已采纳五大分类实时切换与计数 (`FilterToolbar.tsx`)
- [x] **流式分类键盘导航**：在特定筛选视图下，快捷键翻页自动在筛选结果子集内跳转
- [x] **摄影师智能批量操作**：支持“一键采纳所有完美片”、“一键排除所有硬伤”、“一键应用 AI 规则建议”
- [x] **视口智能诊断药丸**：悬浮展示 AI 判定标签与修复建议，支持连拍替换底片一键跳转 (`DefectBadge.tsx`)
- [x] **摄影师主权覆写菜单**：点击诊断药丸呼出下拉菜单，摄影师可随时人工强制覆写 AI 判定
- [x] **选片结果导出弹窗**：多范围筛选导出、目标路径选择、XMP 伴侣绑定、导出进度与结果统计卡片 (`ExportModal.tsx`)
- [x] **顶栏导出选片快捷入口**：实时呈现已入选张数徽标，随时一键调起导出弹窗
- [x] **双图分屏联动比对（Side-by-Side）**：双 WebGL 画布硬件加速并排渲染，保持 60fps 满帧跟手 (`SplitCompareView.tsx`)
- [x] **双向缩放平移同步锁定**：滚轮缩放与鼠标拖拽平移双画布实时联动，支持同步锁定/解锁开闭
- [x] **双图 1:1 特写复位**：一键对焦双图相同位置 100% 像素，毫秒级比对睫毛、瞳孔合焦与细节
- [x] **主备互换与候选遍历**：一键快捷键 `[S]` 互换；右侧候选遍历自动越过左侧主选底片，杜绝死锁卡顿
- [x] **连拍换脸一键调起比对**：在 AI 诊断药丸中点击连拍换脸供体直接进入分屏比对模式 *(本次完成)*
- [x] **独立定夺工具条与快捷键**：左右视口各自独立支持采纳 [P]、排除 [X] 及 0-5 星评级，支持 `C` 键进入/退出、`Esc` 退出 *(本次完成)*
- [x] **连拍组深度选优工作流 (Burst Selection Workflow)**：支持组内限定比对 (`compareScope: 'burst'`)、长按 `[B]` 瞬时闪烁比对、一键定优（`[W]` 设为 5 星 Pick 并自动将同组其余设为 Reject）、单次 `Cmd+Z` 完整撤销与 XMP 事务安全落盘
- [x] **筛选进度感知与序号提示增强**：视口 HUD 相对序号徽章 (`筛选 [3/45]`)、胶片条 `[Seq] #Num` 双显、筛选工具栏实时匹配胶囊、`Home`/`End` 快速首尾跳转
- [x] **开源许可与合规交互弹窗 (About & Licenses)**：顶部栏 LibRaw 徽标交互调起 `AboutModal`，支持内嵌查阅协议全文与动态库自由替换指南

### 1.3 跨端运行时与工程化
- [x] **Tauri v2 IPC 桥接**：打通前端与 Rust 原生底层交互 (`tauriBridge.ts`)
- [x] **Web 纯浏览器降级模式**：提供包含真实业务场景与模拟导出的 Mock 数据
- [x] **跨平台 LibRaw 动态分发 (P1.4)**：自适应构建目标架构，构建动态库搜寻引入 `@executable_path/../Frameworks` 优先 RPATH，同捆 LGPL 2.1 协议与替换指南
- [x] **全链路工程质量门禁 (P1.6)**：建立 GitHub Actions CI 流水线 (`.github/workflows/ci.yml`)，Clippy 零容忍静态分析，制定性能红线与自动化校验脚本 (`npm run gate`)
- [ ] **macOS 自动化公证**：配置 Apple Developer ID 证书与 `notarytool` / `stapler` CI 流水线
- [ ] **Windows 代码签名**：引入 Azure Trusted Signing 或 EV Authenticode 代码签名

---

## 阶段 2：AI 智能筛选与大合影工程化 (Phase 2)

- [x] **连拍时空聚类算法**：基于文件名序号连续性自动识别连拍序列组 (`group_bursts`)
- [x] **光学合焦与锐度检测**：中心区域拉普拉斯算子方差评估，准确剔除不可逆脱焦与运动模糊
- [x] **曝光截断与宽容度分析**：高光死白截断比例判定不可修废片，暗部欠曝判定为后期可提亮
- [x] **连拍换脸跨帧拯救机制**：瑕疵片与同组完美原片供体匹配，自动生成 `[可修: 连拍可换脸/换眼]` 标签
- [x] **严格归一化人脸优先级模型 (Face Priority)**：精确实现 $\text{Priority} = 10 \cdot \text{IsPinned} + 5 \cdot \text{NormArea} - 2 \cdot \text{NormDist}$ (`rules/face.rs`) *(本次完成)*
- [x] **Top 6 特写防界面撑爆**：大合影场景严格截断展示最高优先级 6 张关键人脸切片，兼顾全局与性能 *(本次完成)*
- [x] **多脸联动特写窗格 (Face Loupe)**：底部抽屉呈现 1:1 人脸切片、睁眼率百分比与合焦锐度分值 (`FaceLoupe.tsx`) *(本次完成)*
- [x] **主角人脸钉选与连拍记忆**：摄影师点击 📌 钉选新人主角，连拍组内自动继承特写焦点并置顶展示
- [x] **连拍取消钉选双向同步**：摄影师取消钉选主角时，向同组其他连拍底片对应人脸同步取消钉选
- [x] **大合影闭眼一票否决与气泡报警**：未入选 Top 6 的背景闭眼人物触发浮动报警气泡，支持瞬跳聚焦定位
- [x] **三大摄影场景预设切换**：大合影（一票否决闭眼）、婚礼抓拍（表情与睁眼优先）、肖像摆拍（瞳孔锐度优先）
- [x] **全键盘快捷键覆盖**：快捷键 `[F]` 一键开闭特写抽屉，点击切片画布自动平滑对焦人物
- [ ] **ONNX Runtime 硬件加速推理落地**：集成纯本地 ONNX 引擎，打通 Mac Metal 与 Windows DirectML 硬件加速通道

---

## 阶段 3：NAS 协同与工作室生产力 (Phase 3)

- [x] **群晖 / 威联通 Docker 无头服务**：独立二进制 `quickpick-server` (`src/bin/server.rs`)、跨平台多阶段 `docker/Dockerfile.nas` 与一键部署 `docker/docker-compose.yml`
- [x] **Docker 环境变量与 Daemon 对齐**：`server.rs` 补充读取 `PROXY_MAX_EDGE` 环境变量与 `--max-edge` 命令行参数支持
- [x] **轻量 2K 代理高速缓存规范 (`.quickpick_cache`)**：隔离存储 2K WebP/JPG 代理（单张约 150KB，节约 99.6% 带宽）与预计算 `catalog.json` 索引
- [x] **客户端“零 RAW 传输”极速秒通**：桌面端优先直接加载 150KB 代理缓存（加载延迟 1.2ms，提速 300x），彻底攻克千兆/Wi-Fi SMB 传输物理瓶颈
- [x] **桌面端一键预生成 2K 缓存与状态指示**：顶栏呈现 `⚡ 2K 加速` 绿色徽标与一键预生成 2K 缓存按钮，支持摄影师出差与无 Docker 场景离线选片
- [ ] **Lightroom 目录与 Capture One 导出联动**：一键生成导入 `.lrcat` 或 Capture One 会话文件
